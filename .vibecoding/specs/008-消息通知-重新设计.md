# 通知系统重新设计方案

## 基本信息

- **规格编号**: `SPEC-008`
- **功能名称**: 消息通知（重新设计）
- **优先级**: P1
- **状态**: ✅ 已完成

---

## 一、需求背景

### 1.1 原问题

1. 数据库与模型字段不一致 → API 500 错误
2. 静默规则是全局的，未关联通道
3. 只支持钉钉通道

### 1.2 目标

- 统一通道管理（钉钉/企微/飞书/邮件/Webhook）
- 静默规则绑定到具体通道
- ~~频率限制~~（**已废弃，暂不实现**）

---

## 二、数据模型

### 2.1 通知通道表

```sql
CREATE TABLE notification_channels (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL UNIQUE,
    channel_type VARCHAR(20) NOT NULL,  -- dingtalk/wechat/feishu/email/webhook
    
    -- 通道配置 (JSON格式)
    config JSON NOT NULL,
    -- dingtalk: {webhook, auth_type, secret, keywords}
    -- wechat: {webhook, ...}
    -- feishu: {webhook, ...}
    -- email: {smtp_host, smtp_port, username, password, from_addr}
    -- webhook: {url, method, headers, body_template}
    
    is_enabled BOOLEAN DEFAULT TRUE,
    description VARCHAR(200),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 2.2 静默规则表（关联通道）

```sql
CREATE TABLE channel_silence_rules (
    id SERIAL PRIMARY KEY,
    channel_id INTEGER NOT NULL REFERENCES notification_channels(id) ON DELETE CASCADE,
    name VARCHAR(100) NOT NULL,
    description VARCHAR(200),
    
    -- 匹配条件 (可选)
    alert_level VARCHAR(16),
    instance_id INTEGER,
    metric_type VARCHAR(32),
    
    -- 静默配置
    silence_type VARCHAR(20) DEFAULT 'once',  -- once/daily/weekly
    start_time TIMESTAMP,
    end_time TIMESTAMP,
    time_start VARCHAR(5),        -- HH:MM
    time_end VARCHAR(5),          -- HH:MM
    weekdays INTEGER[],           -- [0-6]
    
    is_enabled BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 三、页面结构

```
通知管理
├── 通道管理
│   ├── 通道列表
│   ├── 新建通道 (选择类型: 钉钉/企微/飞书/邮件/Webhook)
│   └── 通道详情
│       ├── 基本信息
│       ├── 静默规则 Tab
│       └── 绑定配置 Tab
│
├── 通知模板
└── 发送日志
```

---

## 四、实现位置

### 4.1 后端

| 文件 | 说明 |
|------|------|
| `backend/app/api/notification_channels.py` | 通道管理 API |
| `backend/app/api/notification_rules.py` | 静默规则 API |
| `backend/app/api/notification_templates.py` | 通知模板 API |
| `backend/app/api/notification_logs.py` | 通知日志 API |
| `backend/app/models/notification_new.py` | 数据模型 |
| `backend/app/plugins/notification/` | 通知插件 |

### 4.2 前端

| 文件 | 说明 |
|------|------|
| `frontend/src/views/notification/channels.vue` | 通道管理页面 |
| `frontend/src/views/notification/templates.vue` | 模板管理页面 |
| `frontend/src/views/notification/logs.vue` | 发送日志页面 |

### 4.3 数据库表

| 表名 | 说明 |
|------|------|
| `notification_channels` | 通知通道表 |
| `channel_silence_rules` | 通道静默规则表 |
| `notification_templates` | 通知模板表 |
| `notification_logs` | 通知日志表 |

---

## 五、API 列表

### 通道管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/notification/channels` | 获取通道列表 |
| GET | `/api/v1/notification/channels/{id}` | 获取通道详情 |
| POST | `/api/v1/notification/channels` | 创建通道 |
| PUT | `/api/v1/notification/channels/{id}` | 更新通道 |
| DELETE | `/api/v1/notification/channels/{id}` | 删除通道 |
| POST | `/api/v1/notification/channels/{id}/test` | 测试通道 |

### 静默规则

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/notification/channels/{id}/silence-rules` | 获取规则列表 |
| POST | `/api/v1/notification/channels/{id}/silence-rules` | 创建规则 |
| PUT | `/api/v1/notification/channels/{id}/silence-rules/{ruleId}` | 更新规则 |
| DELETE | `/api/v1/notification/channels/{id}/silence-rules/{ruleId}` | 删除规则 |

### 通道绑定

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/notification/channels/{id}/bindings` | 获取绑定列表 |
| POST | `/api/v1/notification/channels/{id}/bindings` | 创建绑定 |
| DELETE | `/api/v1/notification/channels/{id}/bindings/{bindingId}` | 删除绑定 |

---

## 六、废弃功能

> ⚠️ 以下功能已废弃，不再实现

| 功能 | 原因 |
|------|------|
| 频率限制 (rate-limits) | 暂不需要，简化实现 |
| 全局静默规则 | 已改为通道级静默规则 |

---

*最后更新：2026-04-09*
